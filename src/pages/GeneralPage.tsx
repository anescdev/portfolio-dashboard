import LoginProvider from "@utils/LoginProvider";

export default function GeneralPage() {
    return (
        <LoginProvider>
            <h2 className="box" style={{textAlign: "center"}}>Under contruction...</h2>
        </LoginProvider>
    );
}